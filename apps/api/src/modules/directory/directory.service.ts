import {FieldValue} from 'firebase-admin/firestore';import {env,studentOUs,teacherOUs} from '../../config/env.js';import {col} from '../../core/firebase.js';import {googleJson} from '../../integrations/dwd.js';import {isTeacher} from '../people/people.shared.js';const scope=['https://www.googleapis.com/auth/admin.directory.user.readonly'];
type DUser={id:string;primaryEmail:string;name?:{fullName?:string};orgUnitPath?:string;suspended?:boolean};const kind=(p='')=>teacherOUs.some(x=>p.startsWith(x))?'TEACHER':studentOUs.some(x=>p.startsWith(x))?'STUDENT':'OTHER';
export async function syncDirectory(){let token='',count=0;do{const u=new URL('https://admin.googleapis.com/admin/directory/v1/users');u.searchParams.set('customer','my_customer');u.searchParams.set('maxResults','500');if(token)u.searchParams.set('pageToken',token);const d=await googleJson<{users?:DUser[];nextPageToken?:string}>(u.toString(),env.WORKSPACE_ADMIN_SUBJECT,scope);const writer=col('people').firestore.bulkWriter();for(const x of d.users||[]){writer.set(col('people').doc(x.id),{id:x.id,email:x.primaryEmail.toLowerCase(),displayName:x.name?.fullName||x.primaryEmail,orgUnitPath:x.orgUnitPath||'',personType:kind(x.orgUnitPath),suspended:!!x.suspended,source:'DIRECTORY',dataStatus:'COMPLETE',updatedAt:FieldValue.serverTimestamp()},{merge:true});count++}await writer.close();token=d.nextPageToken||''}while(token);return{count}}
export async function teacherEmails(){const s=await col('people').get();return s.docs.filter(d=>isTeacher(d.data())).map(d=>String(d.data().email))}
