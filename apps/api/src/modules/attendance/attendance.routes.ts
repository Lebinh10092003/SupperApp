import {Router} from 'express';import {firebaseAuth,requireCapability} from '../../auth/middleware.js';import {asyncRoute} from '../../core/http.js';import {col} from '../../core/firebase.js';export const attendanceRouter=Router();attendanceRouter.get('/',firebaseAuth,requireCapability('VIEW_STUDENT_DATA'),asyncRoute(async(q,r)=>{const date=String(q.query.date||new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Ho_Chi_Minh'}).format(new Date())),s=await col('meetSessions').where('date','==',date).limit(500).get();r.json({items:s.docs.map(d=>({id:d.id,...d.data()}))})}));